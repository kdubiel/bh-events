import * as common from './common.json';
import * as yup from './yup.json';
import * as events from './events.json';
import * as errors from './errors.json';

export const en_client = {
  common,
  yup,
  events,
};

export const en_api = {
  errors,
};
